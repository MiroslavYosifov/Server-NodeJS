import { expect } from 'chai';

describe('Auth middleware', function (){
    it('test something test', function(){
        const num1 = 2;
        const num2 = 2;
        expect(num1 + num2).to.equal(4);
    });
});
